version: '3.8'

services:
  cacheinference:
    build: .
    container_name: cacheinference
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
